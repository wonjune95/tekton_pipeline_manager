# tekton 관련 설정 커스텀
---
# harbor의 ca인증서
# 변경시 tekton-ctr 재실행
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/instance: default
    app.kubernetes.io/part-of: tekton-pipelines
  name: config-registry-cert
  namespace: tekton-pipelines
data:
  # Registry's self-signed certificate
  cert: |
    {{harbor_ca|indent(4)}}

---
# tekton 기능 최적화 설정
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/instance: default
    app.kubernetes.io/part-of: tekton-pipelines
  name: feature-flags
  namespace: tekton-pipelines
data:
  await-sidecar-readiness: "false"
  coschedule: pipelineruns
  disable-affinity-assistant: "true"
  disable-creds-init: "false"
  enable-api-fields: beta
  enable-cel-in-whenexpression: "false"
  enable-param-enum: "false"
  enable-provenance-in-status: "true"
  enable-step-actions: "false"
  enable-tekton-oci-bundles: "false"
  enforce-nonfalsifiability: none
  keep-pod-on-cancel: "false"
  require-git-ssh-secret-known-hosts: "false"
  results-from: termination-message
  running-in-environment-with-injected-sidecars: "true"
  send-cloudevents-for-runs: "false"
  set-security-context: "false"
  trusted-resources-verification-no-match-policy: ignore
---
# tekton task pod 글로벌 설정 (파이프라인실행시 생성될 task 파드의 배치 노드)
  # dns 관련 설정 추가 가능
  # dns host alias 설정
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/instance: default
    app.kubernetes.io/part-of: tekton-pipelines
  name: config-defaults
  namespace: tekton-pipelines
data:
  default-pod-template: |
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
          - matchExpressions:
            # 노드 개별 지정 대신 노드 그룹 라벨로 선택한다.
            #   kubectl label node <노드명> nodegroup={{node_group|default('tekton-build')}}
            # 노드를 늘릴 때는 라벨만 붙이면 되지만, cicd-local-sc / cicd-store-sc 용
            # /CICD-DATA 디렉토리 준비가 같이 되어야 파드가 PVC 를 붙일 수 있다.
            - key: nodegroup
              operator: In
              values:
              - {{node_group|default('tekton-build')}}
    securityContext:
      fsGroup: 65532