apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/affinity: cookie
    nginx.ingress.kubernetes.io/session-cookie-hash: sha256
    nginx.ingress.kubernetes.io/session-cookie-name: route
  name: {{application_name}}-ing
spec:
  ingressClassName: nginx
  rules:
  - host: {{application_name}}.{{dev_frontend_domain_name}}
    http:
      paths:
      - backend:
          service:
            name: {{application_name}}-svc
            port:
              name: http
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - {{application_name}}.{{dev_frontend_domain_name}}
    secretName: secret-tls